<?xml version="1.0" encoding="UTF-8"?>
<config xmlns="http://www.pactera.com/schema/dipper/xml">
	<mappings>
<!--         <mapping xpath="$.body.LOGIN_NAME" field="userName"/> -->
        <mapping xpath="$.body.MOBILE" field="mobile" must="not"/>
        <mapping xpath="$.body.SIGN_TYPE" field="regType"/>
        <mapping xpath="$.body.MER_NO" field="merNo" must="not"/>
        <mapping xpath="$.body.PASS_WORD" field="loginPwd" must="not"/>
        <mapping xpath="$.body.NAME" field="name" must="not"/>
        <mapping xpath="$.body.CERT_TYPE" field="certType" must="not"/>
        <mapping xpath="$.body.CERT_NO" field="certNo" must="not"/>
        <mapping xpath="$.body.SEX" field="sex" must="not"/>
        <mapping xpath="$.body.EMAIL" field="email" must="not"/>
        <mapping xpath="$.body.ADDRESS" field="address" must="not"/>
        <mapping xpath="$.body.UNION_PLAT_TYPE" field="platformType" must="not"/>
        <mapping xpath="$.body.UNION_PLAT_NO" field="platformUserNo" must="not"/>
        <mapping xpath="$.body.SMS_CODE" field="smsCode" must="not"/>
        <mapping xpath="$.body.USE_FLAG" field="useFlag" must="not"/>
        <mapping xpath="$.body.SID" field="sid" must="not"/>	
		<mapping xpath="$.body.TRANS" field="trans" must="not"/>
		<mapping xpath="$.body.MI_TYPE" field="miType" must="not"/>	
	</mappings>
</config>