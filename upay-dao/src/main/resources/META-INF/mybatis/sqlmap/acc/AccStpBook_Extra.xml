<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.upay.dao.po.acc.AccStpBookPo">
	<select id="lists" resultMap="BaseResultMap">
		 select
		 <include refid="Base_Column"></include>
		 from
		 <include refid="Table_Name"></include>
		 <where>
		 <if test="stpMode!=null">
		 AND STP_MODE=#{stpMode}
		 </if>
		 <if test="stpStat != null">
				STP_STAT= #{stpStat}
				AND
		 </if>
		 <if test="stpStats !=null">
		 AND STP_STAT IN 
		 <foreach item="item" index="index" collection="stpStats"
						 open="(" separator="," close=")">
					#{item}
				</foreach>
		 </if>			
		 </where>	
	</select>
</mapper>